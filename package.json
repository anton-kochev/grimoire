{
  "name": "grimoire-cc-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Collection of specialized agents and skills for Claude Code",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=20.0.0"
  }
}
